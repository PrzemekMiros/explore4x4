<section class="subpage-hero{% if heroClass %} {{ heroClass }}{% endif %}">
  <div class="container subpage-hero-inner">
    <p class="subpage-kicker fade-in">Explore 4x4 / {{ title }}</p>
    <h1 class="split-lines">{{ title }}</h1>
    <p class="subpage-lead fade-in">{{ description }}</p>
  </div>
</section>
